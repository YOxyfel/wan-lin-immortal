{
  "name": "wan-lin-immortal",
  "version": "1.0.0",
  "private": true,
  "description": "Wan & Lin Immortal — a dark xianxia cultivation game website.",
  "type": "module",
  "scripts": {
    "css": "tailwindcss -i ./assets/tailwind.src.css -o ./assets/tailwind.css --minify",
    "css:watch": "tailwindcss -i ./assets/tailwind.src.css -o ./assets/tailwind.css --watch",
    "dev": "npm run css && vite",
    "build": "npm run css && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "vite": "^6.0.0"
  }
}
